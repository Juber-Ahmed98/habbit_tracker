export default function OnboardingPage() {
  return (
    <main className="mx-auto max-w-md px-4 py-6">
      <h1 className="text-[28px] font-semibold text-text">Welcome</h1>
      <p className="mt-2 text-sm text-text-muted">
        Four-step onboarding (name, tabs, goals, connections) lands in step 5.
      </p>
    </main>
  );
}
