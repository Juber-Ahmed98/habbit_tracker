export default function WorkPage() {
  return (
    <section>
      <h1 className="text-[28px] font-semibold text-text">Work</h1>
      <p className="mt-2 text-sm text-text-muted">
        Pomodoro, inbox-zero toggle, and skill-building tracker arrive in step 3.
      </p>
    </section>
  );
}
